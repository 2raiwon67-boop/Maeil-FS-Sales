# 환경변수 / API 키 파일 — 절대 커밋 금지
.env
.env.local
.env.production
config.js
config.local.js
scripts/run-upload.sh

# macOS
.DS_Store

# Node
node_modules/

# 개발/테스트용 파일 (폴더 삭제됨)

# 레시피 PDF 원본 (대용량, Supabase Storage로 관리)
assets/recipe/

# 업무 참고 문서 (로컬 보관)
공공인허가데이터 방법/

# 임시 에러 로그
scripts/upload-errors.json

# 레시피 데이터 (Supabase에 업로드 완료, 재생성 가능)
scripts/recipe-data.json

# Skills 도구 로컬 설정
.agent/
.agents/
skills-lock.json

# Claude Code 로컬 설정 (내부 아키텍처 문서 포함 — 공개 금지)
.claude/
CLAUDE.md
.vercel
